const count = require("./count.js");

test("count function exists", () => {
  expect(typeof filter).toBe("function");
});

test("The result are a Array"),
  () => {
    expect(count()).toBe(Array);
  };
